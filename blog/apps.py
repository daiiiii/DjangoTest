from django.apps import AppConfig
# from suit.apps import SuitConfig

class BlogConfig(AppConfig):
    name = 'blog'


# class SuitConfig(DjangoSuitConfig):
    #  layout = 'horizontal'   #layout决定你的网页是初始样式是垂直样式还是水平样式，可选参数为‘horizontal’或‘vertical’